<!DOCTYPE html>
<html lang="id" data-bs-theme="dark">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= title %> - JWR Admin</title>
    
    <!-- Bootstrap CSS -->
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet">
    
    <!-- Custom Admin CSS (WAJIB) -->
    <link rel="stylesheet" href="/public/css/admin.css">
</head>
<body>

    <header class="admin-header p-3 d-flex justify-content-between align-items-center">
        <img src="/public/logo.webp" alt="Logo" style="height: 40px; margin-right: 10px;">
        <h5 class="mb-0">JWR Admin Panel</h5>
        <div>
            <!-- Tampilkan nama user jika sudah login (opsional) -->
            <% if (locals.user && user.username) { %>
                <span class="text-muted me-3">Halo, <%= user.username %></span>
            <% } %>

            <!-- Arahkan link ke rute logout -->
            <a href="/admin/logout" class="btn btn-outline-danger btn-sm">Logout</a>
        </div>
    </header>